<%= @doctor.name %>

<ul>
<% @doctor.appointments.each do |appointment| %>
<li> Patient: <%= link_to appointment.patient.name, patient_path(appointment.patient) %> Date: <%= appointment.appointment_datetime.strftime("%B %d, %Y at %T")  %> </li>
<% end %>
</ul>
